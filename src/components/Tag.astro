---
import { twMerge } from "tailwind-merge";

interface Props {
  name: string;
  small?: boolean;
}
const { name, small = false } = Astro.props;
---

<a
  href={`/tag/${encodeURI(name)}`}
  class={twMerge("btn btn-secondary", small ? "btn-xs" : "btn-sm")}>#{name}</a
>
